# 文本循环

这是一个独立 ComfyUI 自定义节点插件。

## 安装

将本目录放入：

```text
ComfyUI/custom_nodes/
```

然后重启 ComfyUI。

## 授权

首次运行节点时，在 `授权码` 输入框填写授权码。验证通过后会在本地保存授权状态，后续可以留空运行。

如果更换机器、删除本地授权缓存或授权失效，需要重新填写授权码。

## 节点

- `📝 文本循环`

## 依赖

不需要额外安装第三方 pip 包，依赖 ComfyUI 自带运行环境。
